import { Heart, MessageCircle, Image, User, Send, Rss } from 'lucide-react';

export const Icons = {
  like: Heart,
  comment: MessageCircle,
  image: Image,
  user: User,
  send: Send,
  logo: Rss,
};